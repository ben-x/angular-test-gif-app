import styled from 'styled-components';

const GifBoxDiv = styled.div` 
margin: 0 auto;
display: grid;
grid-template-columns: repeat( auto-fit, minmax(250px, 1fr) );
grid-gap: 20px;
`;

export {GifBoxDiv}
